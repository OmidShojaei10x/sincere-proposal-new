# 🚀 راهنمای آپلود پروژه روی GitHub

## ✅ کارهای انجام شده (خودکار)

1. ✅ **بک‌آپ ایجاد شد**
   - نام فایل: `sincere-backup-20251214_184423.tar.gz`
   - مسیر: `/Users/omid/Downloads/Omid_Shojaei/Proposal/sincere-backup-20251214_184423.tar.gz`
   - حجم: 36MB

2. ✅ **Git Repository آماده شد**
   - Repository initialize شد
   - تمام فایل‌ها commit شدند
   - Branch به `main` تغییر نام داد

---

## 📝 مراحل بعدی (که باید خودتان انجام دهید)

### مرحله 1: ساخت Repository در GitHub

1. **وارد GitHub شوید:**
   - به [github.com](https://github.com) بروید
   - وارد حساب کاربری خود شوید

2. **ساخت Repository جدید:**
   - روی دکمه **"+"** در بالای صفحه کلیک کنید
   - گزینه **"New repository"** را انتخاب کنید

3. **تنظیمات Repository:**
   - **Repository name**: `sincere-proposal` (یا هر نام دیگری که می‌خواهید)
   - **Description**: `پروپوزال تبلیغاتی رستوران سینسیر - شرکت یکتانت`
   - **Visibility**: 
     - ✅ **Public** (اگر می‌خواهید همه ببینند)
     - یا **Private** (اگر می‌خواهید فقط خودتان ببینید)
   - ⚠️ **مهم**: تیک **"Initialize this repository with a README"** را **نزنید** (چون ما قبلاً README داریم)
   - ⚠️ **مهم**: تیک **"Add .gitignore"** را **نزنید** (چون ما قبلاً .gitignore داریم)
   - ⚠️ **مهم**: تیک **"Choose a license"** را **نزنید** (اختیاری است)

4. **ساخت Repository:**
   - روی دکمه **"Create repository"** کلیک کنید

---

### مرحله 2: اتصال Repository محلی به GitHub

بعد از ساخت repository، GitHub یک صفحه با دستورات نشان می‌دهد. **اما** چون ما قبلاً commit کردیم، باید از دستورات زیر استفاده کنیم:

**در Terminal (که در پوشه `sincere` هستید) این دستورات را اجرا کنید:**

```bash
# 1. اضافه کردن remote repository
git remote add origin https://github.com/YOUR_USERNAME/sincere-proposal.git

# 2. Push کردن به GitHub
git push -u origin main
```

⚠️ **توجه**: `YOUR_USERNAME` را با نام کاربری GitHub خودتان جایگزین کنید.

**مثال:**
```bash
git remote add origin https://github.com/omidshojaei/sincere-proposal.git
git push -u origin main
```

---

### مرحله 3: اگر از SSH استفاده می‌کنید

اگر SSH key دارید و می‌خواهید از SSH استفاده کنید:

```bash
git remote add origin git@github.com:YOUR_USERNAME/sincere-proposal.git
git push -u origin main
```

---

### مرحله 4: فعال‌سازی GitHub Pages (اختیاری)

اگر می‌خواهید پروژه به صورت آنلاین در دسترس باشد:

1. به صفحه repository در GitHub بروید
2. روی **"Settings"** کلیک کنید
3. در منوی سمت چپ، **"Pages"** را انتخاب کنید
4. در بخش **"Source"**:
   - **Branch**: `main` را انتخاب کنید
   - **Folder**: `/ (root)` را انتخاب کنید
5. روی **"Save"** کلیک کنید
6. بعد از چند دقیقه، پروژه شما در آدرس زیر در دسترس خواهد بود:
   ```
   https://YOUR_USERNAME.github.io/sincere-proposal/
   ```

---

## 🔧 عیب‌یابی

### اگر خطای "remote origin already exists" دیدید:

```bash
git remote remove origin
git remote add origin https://github.com/YOUR_USERNAME/sincere-proposal.git
```

### اگر خطای Authentication دیدید:

1. **استفاده از Personal Access Token:**
   - به [Settings > Developer settings > Personal access tokens](https://github.com/settings/tokens) بروید
   - یک token جدید با دسترسی `repo` بسازید
   - هنگام push، به جای رمز عبور، token را وارد کنید

2. **یا استفاده از GitHub CLI:**
   ```bash
   gh auth login
   ```

### اگر فایل‌های بزرگ دارید:

اگر فایل‌های خیلی بزرگ (مثلاً عکس‌های بیشتر از 100MB) دارید، ممکن است GitHub اجازه push ندهد. در این صورت:
- فایل‌های بزرگ را از `.gitignore` حذف کنید
- یا از [Git LFS](https://git-lfs.github.com/) استفاده کنید

---

## ✅ چک‌لیست نهایی

- [ ] Repository در GitHub ساخته شد
- [ ] Remote repository اضافه شد
- [ ] فایل‌ها push شدند
- [ ] GitHub Pages فعال شد (اختیاری)
- [ ] پروژه در مرورگر باز شد و کار کرد

---

## 📞 در صورت مشکل

اگر مشکلی پیش آمد:
1. خطای دقیق را کپی کنید
2. به من بگویید تا راهنمایی کنم

---

**موفق باشید! 🎉**

